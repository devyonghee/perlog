const styles = theme => ({
    paper: {
        paddingLeft: theme.spacing.unit * 30,
        paddingTop: theme.spacing.unit * 2,
        minHeight: '100vh',
    },

    message: {



    },

    contents: {
        paddingLeft: '10px',
        h1: true,
    }
});

export default styles;